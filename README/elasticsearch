sudo docker-compose up --build --> docker-composeni run qilish
sudo systemctl stop postgresql --> agar postgres ishlab turgan bo'lsa shuni o'chirib keyin run qilish kerak
sudo docker-compose ps --> bizdagi ishlagan yoki ishlab turgan dockerlarni korstaib turadi
sudo docker exec -it db psql -U <database user> <database name> -- docker dagi db ga kiradi.
sudo docker-compose -f docker-compose.yml exec django python manage.py search_index --create --> for create indexing
sudo docker-compose -f docker-compose.yml exec django python manage.py search_index --rebuild --> for rebuild indexing
sudo docker-compose -f docker-compose.yml exec django python manage.py makemigrations --> for makemigrations
sudo docker-compose -f docker-compose.yml exec django python manage.py migrate --> for migrate
